<div class="comment-form">
    {{#each this}}
    <div class="comment">
        <div class="created-date">
            {{user.username}} commented on {{format_date}}
        </div>
        <div class="text">
            {{comment_content}}
        </div>
    </div>
    {{/each}}
</div>

<div class="post-post-info">
    <div class="post-card">
        <div class="info-div">
            <div class="post-title">
                <a href="/post/{{id}}">{{title}}</a>
            </div>
            <div class="created-user">
                <p id="comment-info">
                    post created by {{user.username}} on {{format_date}}
                </p>
            </div>
        </div>
        <div class="post-content">
            <p>{{content}}</p>
        </div>
    </div>
</div>